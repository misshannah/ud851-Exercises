<?xml version="1.0" encoding="utf-8"?>

<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- DONE (2) Create a res->bools.xml file and create a boolean for this default value -->
    <!-- DONE (3) Update these values to be values from resource files and not hard coded -->

    <CheckBoxPreference
        android:defaultValue="true"
        android:key="@string/pref_bass_value"
        android:summaryOff="@string/summary_off"
        android:summaryOn="@string/summary_on"
        android:title="@string/pref_bass_title" />

</PreferenceScreen>
